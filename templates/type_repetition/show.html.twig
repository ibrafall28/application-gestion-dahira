{% extends 'accueil.html.twig' %}

{% block title %}TypeRepetition{% endblock %}

{% block body %}
    <h1>TypeRepetition</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_repetition.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_repetition.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_repetition_index') }}">back to list</a>

    <a href="{{ path('type_repetition_edit', {'id': type_repetition.id}) }}">edit</a>

    {{ include('type_repetition/_delete_form.html.twig') }}
{% endblock %}
