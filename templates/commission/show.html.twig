{% extends 'base.html.twig' %}

{% block title %}Commission{% endblock %}

{% block body %}
    <h1>Commission</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commission.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ commission.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commission_index') }}">back to list</a>

    <a href="{{ path('commission_edit', {'id': commission.id}) }}">edit</a>

    {{ include('commission/_delete_form.html.twig') }}
{% endblock %}
