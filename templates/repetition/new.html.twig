{% extends 'accueil.html.twig' %}

{% block title %}New Repetition{% endblock %}

{% block body %}
    {% for message in app.flashes('danger') %}
        <div class="alert alert-danger">
            {{ message }}
        </div>
    {% endfor %}
    {{ include('repetition/_form.html.twig') }}

    <a href="{{ path('repetition_index') }}">back to list</a>
{% endblock %}
