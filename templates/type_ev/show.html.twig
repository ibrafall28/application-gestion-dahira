{% extends 'base.html.twig' %}

{% block title %}TypeEv{% endblock %}

{% block body %}
    <h1>TypeEv</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_ev.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_ev.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_ev_index') }}">back to list</a>

    <a href="{{ path('type_ev_edit', {'id': type_ev.id}) }}">edit</a>

    {{ include('type_ev/_delete_form.html.twig') }}
{% endblock %}
