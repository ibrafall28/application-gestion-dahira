{% extends 'base.html.twig' %}

{% block title %}Dadj{% endblock %}

{% block body %}
    <h1>Dadj</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dadj.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ dadj.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dadj_index') }}">back to list</a>

    <a href="{{ path('dadj_edit', {'id': dadj.id}) }}">edit</a>

    {{ include('dadj/_delete_form.html.twig') }}
{% endblock %}
